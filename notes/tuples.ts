const drink = {
    color: 'brown',
    carbonated: true,
    sugar: 40
}

type Vax = [string, boolean, number];

const initial_shot : Vax = ['moderna', false, 666];
const dumb_shot : Vax = ['pfizer', true, 6661];
